"""feed_enricher — прокси-сервис для обогащения фидов ProfitBase.

Цикл:
  1) парсим оригинальный фид PB (XML)
  2) для каждого лота генерим обогащенную планировку
     (шаблон из Figma + ориг. план + данные лота + УТП)
  3) кладём PNG на статичный хостинг (получаем публичный URL)
  4) собираем новый XML с подмененными photo_url
  5) отдаём по статичному URL для CIAN / Avito / Yandex

Запуск:
  python -m feed_enricher.cli refresh       # обновить кэш планировок
  python -m feed_enricher.cli serve         # запустить HTTP-сервер
"""
__version__ = "0.1.0"
