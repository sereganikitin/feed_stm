# feed_enricher — гайд по фидам (для передачи / нового компа)

Сервис берёт данные из **ProfitBase** (pb7828.profitbase.ru), накладывает наши
брендовые планировки/фото/тексты и отдаёт готовые XML-фиды классифайдам
(ЦИАН, Авито, Яндекс, ДомКлик). Всё крутится на прод-сервере, обновляется по
расписанию и по кнопке в админке.

Прод: **https://feeds.infoseledka.ru** · Проекты: **Зорге 9** (`zorge9`,
апартаменты) и **Квартал Серебряный Бор / Берзарина 37** (`b37`, квартиры) +
коммерция.

---

## 1. Где что лежит

| Что | Где |
|---|---|
| Git-репозиторий | `github.com/sereganikitin/feed_stm` (ветка `main`) |
| Локальный клон (этот комп) | `C:\Users\Рыба\Downloads\Telegram Desktop\feed_enricher_extracted\` |
| Прод-сервер | `72.56.241.199` (root), код в `/opt/feed-enricher/`, Docker-контейнер `feed-enricher` |
| SSH-ключ | `~/.ssh/id_ed25519` (`ssh root@72.56.241.199`) |
| Публичный домен | `feeds.infoseledka.ru` (nginx → контейнер :8765, TLS Let's Encrypt) |
| Админ-панель | `https://feeds.infoseledka.ru/admin` (пароль в `.env` сервера) |

**Секреты (НЕ в git, только в `.env` на сервере `/opt/feed-enricher/.env`):**
`ADMIN_PASSWORD` (вход в админку), `PB_API_KEY` (ProfitBase API — нужен для
коммерции Зорге/Б37), `PB_AVITO_FEED_URL_*`, `FLASK_SECRET_KEY`, `PUBLIC_BASE_URL`.

> На новом компе: `git clone https://github.com/sereganikitin/feed_stm.git`.
> Пуш по HTTPS через сохранённый токен GitHub (аккаунт sereganikitin). SSH к
> серверу — скопировать `~/.ssh/id_ed25519` со старого компа.

---

## 2. Все фиды (ссылки)

### Жилые (Зорге / Б37)
| Площадка | Зорге | Б37 |
|---|---|---|
| ЦИАН | `/feed/zorge9.xml` | `/feed/b37.xml` |
| Авито | `/feed/zorge9-avito.xml` | `/feed/b37-avito.xml` |
| Яндекс.Недвижимость (старый realty/2010-06) | `/feed/zorge9-yandex.xml` | `/feed/b37-yandex.xml` |
| Яндекс Поиск Недвижимость (новый metarealty/2024-12) | `/feed/zorge9-yandex-realty.xml` | `/feed/b37-yandex-realty.xml` |
| **Яндекс Поиск — ОБЩИЙ (Зорге+Б37 одним файлом)** | `/feed/yandex-realty.xml` | — |
| ДомКлик | `/feed/zorge9-domclick.xml` | `/feed/b37-domclick.xml` |

### Коммерция
| Фид | URL | Что внутри |
|---|---|---|
| Зорге ЦИАН | `/feed/comm/zorge-cian.xml` | 15 лотов Зорге + 2 машиноместа |
| Б37 аренда ЦИАН | `/feed/comm/b37-rent-cian.xml` | 5 лотов аренды Б37 |
| Авито коммерция (общий) | `/feed/comm/avito.xml` | Зорге + Б37 + машиноместа |
| **Классифайд Зорге ЦИАН** | `/feed/comm/zorge-cls-cian.xml` | свои тексты/порядок: 9 аренда → 6 продажа → 2 ММ → 5 Б37 |
| **Классифайд Зорге Авито** | `/feed/comm/zorge-cls-avito.xml` | то же для Авито |

Все фиды видны в дашборде `/admin`.

---

## 3. Как деплоить

Код в контейнер COPY-ится, поэтому нужна пересборка/подмена. Обычный путь:

```bash
# из корня локального репо: скопировать изменённые файлы на сервер
scp feed_enricher/<файл>.py root@72.56.241.199:/opt/feed-enricher/feed_enricher/
ssh root@72.56.241.199 'cd /opt/feed-enricher && docker compose build feed-enricher && docker compose up -d feed-enricher'
```

**Если Docker Hub отдаёт 429 (Too Many Requests)** — базовый образ не
скачивается, `build` падает. Обход (подменяем файл в живом контейнере и
фиксируем образ снапшотом):

```bash
scp feed_enricher/<файл>.py root@72.56.241.199:/tmp/<файл>.py
ssh root@72.56.241.199 '
  cp /tmp/<файл>.py /opt/feed-enricher/feed_enricher/<файл>.py      # для будущего чистого билда
  docker cp /tmp/<файл>.py feed-enricher:/app/feed_enricher/<файл>.py
  docker commit feed-enricher feed-enricher:latest                  # снапшот → переживёт пересоздание
  docker restart feed-enricher'                                     # рестарт нужен, если менялся .py
```

- **Данные (json/картинки/шрифты)** — так же `docker cp`, но рестарт НЕ нужен, если
  модуль читает файл при каждой сборке (напр. тексты классифайда). Если модуль
  грузит файл при импорте — нужен рестарт.
- После деплоя дёргаем нужный refresh (см. ниже) и проверяем фид `curl`-ом.

**Триггеры пересборки (POST):**
`/refresh/<slug>` (жилой проект), `/refresh-comm-zorge`, `/refresh-comm-rent`,
`/refresh-comm-avito`, `/refresh-comm-zorge-cls`. На старте контейнера — авто-refresh
всего; плюс фоновый цикл раз в `REFRESH_INTERVAL_HOURS`.

---

## 4. Частые правки — куда смотреть

| Задача | Где менять |
|---|---|
| Тексты классифайд-фида Зорге | Google Doc → перегенерить `feed_enricher/comm_zorge_cls_texts.json` (см. §6), задеплоить json, `POST /refresh-comm-zorge-cls` |
| Порядок лотов классифайда | `comm_zorge_classified.ORDER` |
| Состав машиномест | `comm_parking.LOTS` (список `(ExternalId, [номера], семейное?)`) |
| Координаты строений Зорге (ЦИАН) | `comm_zorge_cian.COORD_BY_LOT` / `ZK_COORD` |
| Координата паркинга (ЦИАН) | `comm_parking.COORD` |
| Координата Б37 (ЦИАН) | `comm_cian_rent` (константа в `refresh`) |
| Картинка машиномест / плашки | `feed_enricher/assets/parking.jpg` + `comm_parking._LAY_SHORT/_LAY_LONG` (координаты плашек) |
| Атрибуты Авито-коммерции (отделка/охрана/парковка/вход/тип аренды и т.д.) | Админка `/admin/comm-avito` (редактируемые дефолты) |
| Фото карточек жилых (Авито/Яндекс/ЦИАН) | Админка `/admin/<slug>` (загрузка/порядок/синк с Я.Диска) |
| Настройки жилого проекта (отделка, рассрочка, скидка) | Админка `/admin/<slug>` |
| Виды из окон | Админка `/admin/<slug>/views` |
| Состав/назначения коммерции Зорге | `comm_zorge_cian.LISTINGS` (фикс-список из ProfitBase API) |
| Адреса строений Зорге | `comm_zorge_cian.ADDR_BY_LOT` |
| Шаблоны планировок Б37 (студии/квартиры) | `feed_enricher/assets/templates/b37_*.jpg` + `config.py` layout b37 |

---

## 5. Ключевые модули (`feed_enricher/`)

- **`server.py`** — Flask-приложение: все роуты фидов, `refresh_project`,
  `resync_views`, фоновый цикл, генерация жилых фидов.
- **`config.py`** — `PROJECTS` (zorge9/b37): URL ProfitBase, id Яндекс/ДомКлик,
  layout планировок, наборы фото, координаты и т.д. Оверрайды из админки — в
  `cache/admin/overrides.json`.
- **`parser.py`** — парсинг ЦИАН-XML ProfitBase → `FeedLot`.
- **`enricher.py`** — рисует брендовую планировку (шаблон + план + подписи
  комнаты/площадь/этаж; Б37 — свой шаблон студии/квартиры + название башни).
- **`assembler.py` / `assembler_avito.py` / `assembler_yandex.py` /
  `assembler_yandex_realty.py` / `assembler_domclick.py`** — сборка жилых фидов
  под каждую площадку.
- **`comm_zorge_cian.py`** — коммерция Зорге (ЦИАН) из ProfitBase API по
  `LISTINGS`; адреса/координаты по строениям.
- **`comm_cian_rent.py`** — коммерция аренда Б37 (ЦИАН): нативный экспорт + цены
  из API.
- **`comm_avito.py`** — конвертер коммерции ЦИАН→Авито (категория «Коммерческая
  недвижимость» / «Гаражи и машиноместа»); дефолты атрибутов редактируются в
  админке.
- **`comm_parking.py`** — машиноместа Зорге (обычные + семейные) из отдельного
  фида паркинга; рендер плашек номер/площадь на `parking.jpg`.
- **`comm_zorge_classified.py`** — классифайд-фиды Зорге (свои тексты + порядок);
  переупорядочивает готовый `comm_zorge_cian.OUT`, меняет описания, дописывает
  машиноместа и Б37.
- **`admin.py`** — админ-панель (`/admin`): дашборд, фото карточек, настройки,
  атрибуты Авито-коммерции, виды из окон.
- **`yadisk.py`** — синк фото с Яндекс.Диска (зеркалирование папок).

Прод-данные (планировки, кэш фидов) — в Docker-volume `feed-cache`
(`/app/feed_enricher/cache/`), переживают пересборку.

---

## 6. Как обновить тексты классифайд-фида Зорге

Тексты берутся из Google Doc и вшиты в `comm_zorge_cls_texts.json`
(ExternalId → текст). Порядок блоков: 9 аренда, затем 6 продажа
(Фитнес, ГАБ371, Йога 4/3/2 эт, ГАБ894 — у каждого свой текст).

1. Скачать актуальный текст (curl проносит редирект Google, в отличие от
   некоторых http-клиентов):
   ```bash
   curl -sL "https://docs.google.com/document/d/1oAJANcpuanLgo7dVPUKWUSwBX2eDGwcGQe46MwVy6sA/export?format=txt" -o texts.txt -A "Mozilla/5.0"
   ```
2. Распарсить по блокам `N.` (regex `^(\d+(?:\.\d+)*\.\s*)+` убирает ведущую
   нумерацию, в т.ч. двойную «3. 3.1.») и сопоставить с ExternalId по площади/этажу
   (аренда/продажа порядок — см. `comm_zorge_classified.ORDER`).
3. Записать `feed_enricher/comm_zorge_cls_texts.json`, задеплоить (`docker cp`),
   `POST /refresh-comm-zorge-cls`. Рестарт НЕ нужен (тексты читаются per-build).

ЦИАН описание ≤3000 симв (усекается по границе предложения), Авито ≤7500.

---

## 7. Нюансы площадок (грабли, которые уже прошли)

**ЦИАН**
- Валидатор — только кабинет ЦИАН (импорт фида → отчёт). Публичного «вставь XML»
  чекера нет.
- Тег назначения = `Specialty` (амер., без лишней «i»), значения — технические id
  (см. `config.SPECIALTY_ID`), не русские подписи.
- Описание: `&` запрещён (→ «и»), `«»` → `"`, `–` → `-`, `№ / \` убираются, 15–3000 симв.
- **Машиноместо** — категория `garageSale`, блок `<Garage><Type>parkingPlace</Type>
  <Status>ownership</Status></Garage>` (GarageType/Material — только для гаражей-боксов,
  машиноместу НЕ нужны); `BargainTerms` = Price + Currency + `ContractType=sale`.
- **Координаты** `<Coordinates><Lat><Lng>` — точнее геокодирования; передаём по
  строениям (только ЦИАН, не Авито).
- **Адрес блокируется на правку через 2 суток** после публикации. При смене адреса
  (напр. 9Ак1→9Ас5) просить поддержку разблокировать на пару суток и перезалить.

**Авито (Автозагрузка)**
- Валидатор — кабинет Автозагрузки (отчёт по каждому объявлению).
- У каждой категории свой набор полей и справочники значений. Разбирали:
  «Коммерческая недвижимость - Продам» и «- Сдам» (у аренды свои `RentalType=Прямая`,
  `PriceType=в месяц`, `Entrance`, у офиса `OfficeType=Помещение под офис` +
  `Layout=Кабинетная`), «Гаражи и машиноместа» (`Secured`=Да/Нет — НЕ Security;
  `ObjectSubtype=Подземный паркинг`; Square необязателен, строгий список — не шлём).
  `Heating` не принимает «Есть» (→ «Центральное»). `Floor` — список
  (−1→Цокольный). Дефолты редактируются в `/admin/comm-avito`.
- **Дубли**: Авито блокирует похожие объявления (адрес + характеристики + фото).
  Поэтому у машиномест на фото рисуются плашки с номером/площадью — каждое фото
  уникально. Если грузить в один аккаунт несколько наших фидов с одинаковыми Id —
  будут дубли.

**Яндекс**
- Старый realty/2010-06 и новый metarealty/2024-12 (Яндекс Поиск) — разные
  форматы. В новом: `deal-status=продажа от застройщика`, апартаменты тегом
  `<apartment>`, студии без `<rooms>`, `yandex-house-id` обязателен (при новом
  корпусе — добавить маппинг в `config.yandex_house_ids`, иначе оффер отклонят —
  проверка на дашборде подсветит).

**ДомКлик** — берём готовый DomClick-экспорт ProfitBase (правильные id корпусов),
подменяем только планировки/фото.

---

## 8. Правило «новые лоты во все фиды»

Жилые фиды пересобираются из ProfitBase на каждом refresh → новые квартиры в
существующих корпусах подхватываются САМИ. Требуют ручного действия только:
(1) новый КОРПУС — добавить `yandex_house_ids` в config + завести на ДомКлике;
(2) кодовые коммерческие фиды Зорге (`comm_zorge_cian.LISTINGS` — фикс-список).

---

_Гайд отражает состояние на август 2026. Подробная история решений — в коммитах
git и в памяти ассистента (`project_feed_enricher`)._
